# Agent 09 verification record (2026-07-24)

External URLs:

- webmarsimulator.com: 307 redirect to https://www.webmarsimulator.com/ which returns 200
- getstayfitapp.com: 200

/projects/webmars:

- metrics 15,000 / ~50 / 100+ / 0 rendered and counting
- role renders "Core-engine lead"; attribution note ("owned by the
  Webmarssimulator organization") visible on the page
- org repo link github.com/Webmarssimulator/WebMARS present (repo is public,
  verified via authenticated API in Agent 05)
- all three hard parts verbatim (circular snapshot history, sign-extension
  and address-resolution, the decision the whole project rests on)

/projects/futures-engine:

- metrics 8 / 10,000+ / 100% / Java 21 rendered
- PRIVATE badge, zero links to Futures-Trading-Algorithm anywhere
- all three hard parts verbatim (flatten-by-time and kill-switch, block
  bootstrapping, observable while it runs)

/projects/stayfit-website:

- metric wall correctly ABSENT (content block defines none)
- "The hard parts" and "What I built" correctly OMITTED (owner input
  pending); problem section + live link + outcomes render
- getstayfitapp.com link present

Prev/next cycles across all four:
stayfit -> stayfit-website -> webmars -> futures-engine -> stayfit
